import styles from "./Header.module.scss";

const Header = () => {
  return (
    <header className={styles.container}>
      <div>Connor Musson - Assessment</div>
    </header>
  );
};

export default Header;
